# YOUTH ECHO 현장 운영·장애 대응 매뉴얼

**대상:** 꿈제작소 청년 대상 AR 게임 행사 운영진  
**버전:** 2026 행사 운영안  
**목표:** 참가자 개인정보를 수집하지 않으면서도, 네트워크 장애 상황에서 체험과 경품 수령을 중단하지 않는다.

## 1. 운영 원칙

YOUTH ECHO의 게임 진행 기록은 참가자 휴대폰의 브라우저에만 저장한다. 운영 서버는 경품 중복 수령을 막는 데 필요한 **해시된 입장권 번호, 해시된 완주 코드, 발급·수령 상태와 시각**만 보관한다. 이름, 전화번호, 이메일, 카메라 영상, GPS 이동 이력, 코드 원문은 서버에 저장하지 않는다.

> 데이터 최소화는 불필요한 개인정보가 무단 접근 또는 사용에 노출될 가능성을 줄이고 신뢰를 높인다.[1]

| 구분 | 온라인 정상 운영 | 네트워크 장애 운영 |
|---|---|---|
| 게임 진행 | 현장 AR 및 기기 내 저장 | 이미 열린 게임은 기기 내 기록으로 계속 진행 가능 |
| 완주 확인 | 운영자가 완주 화면 확인 | 운영자가 완주 화면 또는 스토리 최종 화면 확인 |
| 경품 처리 | 운영자 화면에서 1회용 코드 발급·수령 처리 | 종이 대장에 입장권 번호와 수령 상태만 기록 |
| 중복 방지 | 서버의 코드 상태가 즉시 차단 | 물리적으로 보관하는 입장권 절취·도장·대장 3중 확인 |
| 복구 | 서버 상태 확인 후 재개 | 네트워크 복구 뒤 대장을 대조하고 중복 없는 항목만 처리 |

## 2. 네트워크·서버 장애 오프라인 대응

### 2.1 사전 준비

행사 시작 전, 운영자는 현장 와이파이와 LTE를 각각 시험한다. 경품 부스에는 최소 두 가지 네트워크 경로를 준비한다. 예를 들어 행사 와이파이를 기본으로 두고, 운영자 휴대폰의 테더링을 예비 경로로 둔다. 완전한 단절에 대비해 **순번이 인쇄된 종이 입장권**, **오프라인 수령 대장**, **완주 확인 도장 또는 스티커**, **펜과 예비 배터리**를 비치한다.

기술 장애 대응은 단순히 “인터넷이 다시 연결되면 처리한다”가 아니라, 누가 장애를 선언하고, 어떤 종이 기록을 남기며, 누가 복구 뒤 대조할지를 미리 정하는 절차가 필요하다. NIST는 운영 우선순위와 요구 사항을 사전에 평가하는 비상계획 수립을 권고하며, CISA는 실제 상황을 가정한 테이블탑 연습으로 역할·의사소통·복구 절차를 점검하도록 안내한다.[2] [3]

| 준비물 | 수량 기준 | 운영 목적 |
|---|---:|---|
| 종이 입장권 | 예상 참가자 수 + 15% | 무작위 번호로 익명 참가·경품 처리 |
| 오프라인 수령 대장 | 2부 | 입장권 번호·완주 확인·수령 여부 기록 |
| 완주 확인 스티커 또는 도장 | 2종 이상 | 체험 부스 확인과 경품 부스 확인 분리 |
| 예비 네트워크 | 와이파이 + LTE 테더링 | 단일 회선 장애 회피 |
| 보조배터리·충전선 | 운영 기기별 1세트 이상 | 운영자 기기 지속 사용 |
| 사고 연락표 | 1장 | 총괄·기술 담당·공간 담당 즉시 연결 |

### 2.2 장애 발생 즉시 절차

장애를 발견한 운영자는 먼저 “경품 코드 발급 일시 중지”를 선언하고, 부스 안내문을 **오프라인 확인 중**으로 바꾼다. 참가자의 게임을 처음부터 다시 시키지 않는다. 체험 부스 담당자는 완주 화면을 확인한 뒤 입장권에 첫 번째 확인 도장 또는 스티커를 붙인다. 경품 부스 담당자는 동일 입장권의 번호를 오프라인 대장에 기록하고, 경품을 수령 처리한 뒤 두 번째 표시를 남긴다.

| 단계 | 체험 부스 | 경품 부스 | 총괄·기술 담당 |
|---|---|---|---|
| 1. 선언 | 완주 코드 발급 중지 안내 | 온라인 수령 처리 중지 | 장애 시각·증상 기록 |
| 2. 확인 | 완주 화면 + 입장권 번호 확인 | 입장권의 체험 확인 표시 확인 | 와이파이·LTE·서비스 상태 구분 |
| 3. 수령 | 첫 확인 도장 또는 스티커 | 대장 기록 + 두 번째 표시 + 경품 전달 | 10분마다 복구 현황 공유 |
| 4. 복구 | 신규 참가자부터 온라인 흐름 재개 | 종이 대장과 서버 발급 기록 대조 | 중복·누락 예외만 사후 처리 |

### 2.3 중복 경품 방지 규칙

오프라인 상태에서는 서버의 실시간 중복 차단이 작동하지 않는다. 따라서 다음 세 가지를 동시에 사용한다.

1. 입장권의 무작위 번호가 이미 대장에 있는지 확인한다.
2. 체험 부스의 1차 확인 도장·스티커가 있는지 확인한다.
3. 경품 전달 뒤 입장권을 절취하거나, 번호 옆에 2차 수령 표시를 남긴다.

복구 뒤에는 오프라인 대장의 번호를 운영자 화면에서 하나씩 대조한다. **오프라인 수령분에 대해 새 온라인 코드를 다시 발급하지 않는다.** 번호가 중복되거나 대장에 누락된 경우에는 경품을 보류하고 총괄이 종이 입장권·완주 확인 표시를 기준으로 판단한다.

## 3. 실제 완주 코드 구현 구조

### 3.1 시스템 구성

참가자 웹앱은 공개 API로 경품 코드를 생성하지 않는다. 경품 코드는 운영자 전용 `/operator` 화면에서만 발급·수령 처리한다. 운영자 화면은 로그인한 관리자 권한을 확인하고, 서버는 난수 생성·해시 비교·만료·중복 수령을 처리한다.

```text
참가자 휴대폰
  └─ 게임 진행 기록: 브라우저 localStorage
  └─ 완주 화면 제시
                 │
                 ▼
운영자 화면 /operator (관리자 로그인)
  └─ 종이 입장권 번호 확인
  └─ 1회용 코드 발급 / 경품 수령 처리
                 │
                 ▼
서버 + DB
  └─ participationHash / codeHash / 상태 / 발급·수령 시각
```

### 3.2 데이터베이스 스키마

실제 구현은 `completion_codes` 테이블에 해시만 저장한다. `participationHash`는 종이 입장권 또는 배부 QR의 무작위 번호를 SHA-256으로 해시한 값이고, `codeHash`는 경품 코드의 SHA-256 해시다. 두 컬럼 모두 유일 제약을 두어 같은 입장권 또는 동일 코드가 두 번 발급되는 것을 막는다.

```ts
// drizzle/schema.ts
export const completionCodes = mysqlTable("completion_codes", {
  id: int("id").autoincrement().primaryKey(),
  participationHash: varchar("participationHash", { length: 64 }).notNull().unique(),
  codeHash: varchar("codeHash", { length: 64 }).notNull().unique(),
  status: mysqlEnum("status", ["issued", "redeemed", "expired"])
    .default("issued").notNull(),
  issuedAt: timestamp("issuedAt").defaultNow().notNull(),
  expiresAt: timestamp("expiresAt").notNull(),
  redeemedAt: timestamp("redeemedAt"),
});
```

| 컬럼 | 보관 내용 | 개인정보 여부 | 용도 |
|---|---|---|---|
| `participationHash` | 입장권 번호의 해시 | 비식별 값 | 같은 입장권의 재발급 차단 |
| `codeHash` | 완주 코드의 해시 | 비식별 값 | 1회 수령·상태 확인 |
| `status` | issued / redeemed / expired | 없음 | 경품 상태 |
| `issuedAt`, `expiresAt`, `redeemedAt` | 서버 시각 | 없음 | 만료와 장애 대응 대조 |

### 3.3 코드 생성·발급·수령 처리

코드는 `node:crypto`의 `randomBytes`로 생성한다. 모호한 문자 `0`, `O`, `1`, `I`는 제외해 현장 구두 전달과 대장 기록 오류를 줄였다. 생성된 원문 코드는 발급 응답에서만 반환하고 DB에는 저장하지 않는다.

```ts
// server/db.ts — 코드 생성
export function createPrizeCode() {
  const alphabet = "ABCDEFGHJKLMNPQRSTUVWXYZ23456789";
  const bytes = randomBytes(16);
  const groups = Array.from({ length: 4 }, (_, group) =>
    Array.from({ length: 4 }, (_, index) =>
      alphabet[bytes[group * 4 + index] % alphabet.length]
    ).join("")
  );
  return `YE-${groups.join("-")}`;
}
```

```ts
// server/routers.ts — 운영자 전용 발급 API
completionCode: router({
  issue: adminProcedure
    .input(z.object({
      participationToken: z.string().trim().min(8).max(80),
      validHours: z.number().int().min(1).max(72).default(24),
    }))
    .mutation(({ input }) =>
      issueCompletionCode(
        input.participationToken,
        new Date(Date.now() + input.validHours * 60 * 60 * 1000)
      )
    ),
  redeem: adminProcedure
    .input(z.object({ code: z.string().trim().min(8).max(32) }))
    .mutation(({ input }) => redeemCompletionCode(input.code)),
});
```

`redeemCompletionCode`는 아직 수령되지 않았고 만료되지 않은 레코드에만 `redeemedAt`을 기록한다. 조건부 업데이트의 영향 행이 1이 아닐 때는 이미 처리된 코드로 반환한다. 이 방식은 두 운영자가 같은 코드를 동시에 처리하는 위험을 줄인다.

> OWASP는 식별자에 충분한 엔트로피를 두고, 값에 개인정보나 의미를 넣지 않으며, 만료와 HTTPS 전송을 관리하도록 권고한다.[4]

### 3.4 로그·보관 정책

운영 로그에는 코드 원문·세션값·전화번호·GPS 좌표를 기록하지 않는다. 발급/수령 성공·실패, 시각, 상태, 운영자 권한 실패처럼 실제 운영에 필요한 사건만 남긴다. 행사 종료 뒤 보관 기한을 정해 해시와 운영 기록을 삭제한다. 경품 정산·이의 제기 기간이 끝난 직후 삭제하는 방식을 권장한다.

OWASP는 로그에 세션 식별값, 인증 토큰, 민감 개인정보를 직접 기록하지 않거나 해시·마스킹하도록 권고한다.[5]

## 4. 청년 행사 운영진 체크리스트

### 4.1 개장 전

| 확인 영역 | 확인 항목 | 담당 | 완료 |
|---|---|---|---|
| 역할 | 총괄·체험·경품·기술·공간 안전 담당자가 서로를 알고 있는가 | 총괄 | □ |
| 기기 | 운영자 로그인, 코드 발급·수령, 충전, 예비 기기 테스트를 마쳤는가 | 기술 | □ |
| 네트워크 | 와이파이와 LTE 테더링 모두 접속·전환 시험을 했는가 | 기술 | □ |
| 오프라인 | 종이 입장권, 대장 2부, 도장/스티커, 펜, 예비 배터리가 있는가 | 경품 | □ |
| 체험 | 카메라·GPS 권한 실패 시 테스트 체험 안내가 준비됐는가 | 체험 | □ |
| 안내 | 개인정보 미수집, 기기 내 저장, 데이터 삭제 안내가 화면과 현장에 있는가 | 안내 | □ |
| 접근성 | 앉아서 체험할 자리, 보조 안내, 밝기·소음·동선이 준비됐는가 | 공간 | □ |

### 4.2 운영 중

| 상황 | 운영 기준 | 담당 |
|---|---|---|
| 참가자 대기 | 한 번에 체험할 인원과 대기 동선을 명확히 안내 | 안내 |
| 권한 거부 | 테스트 체험 또는 운영자 보조로 전환, 강제 설치·로그인 요구 금지 | 체험 |
| 완주 확인 | 완주 화면과 입장권 번호를 둘 다 확인 | 체험 |
| 경품 처리 | 온라인이면 코드 1회 수령 처리, 오프라인이면 대장·도장·절취 3중 확인 | 경품 |
| 네트워크 장애 | 코드 발급 중지 선언 → 종이 대장 흐름 전환 → 10분 간격 상태 공유 | 총괄·기술 |
| 안전·갈등 | 참가자 불편·개인정보 질문·군중 밀집은 총괄에게 즉시 이관 | 전원 |

### 4.3 마감 후

| 확인 영역 | 마감 절차 | 담당 |
|---|---|---|
| 경품 대조 | 서버 수령 기록과 오프라인 대장을 대조하고 예외만 분리 | 경품·총괄 |
| 종이 기록 | 정산 기간까지 잠금 보관 후 정해진 날짜에 파기 | 총괄 |
| 운영자 권한 | 공유 기기에서 운영자 로그아웃, 테더링 종료, 비밀번호 공유 금지 | 기술 |
| 데이터 | 코드 해시·운영 기록의 삭제일을 확정하고 이행 담당 지정 | 총괄 |
| 회고 | 네트워크·권한·대기·경품 예외를 15분 내 기록해 다음 행사 개선 | 전원 |

## 5. 행사 전 20분 리허설 시나리오

행사 개장 전에 다음 순서로 20분 리허설을 실시한다. 첫 5분에는 정상 네트워크에서 운영자 코드 발급·수령을 한 번씩 시험한다. 다음 5분에는 와이파이를 끄고 LTE로 전환한다. 이어지는 5분에는 인터넷을 모두 끈 상태에서 종이 대장·도장·경품 수령 흐름을 실제로 한 번 수행한다. 마지막 5분에는 복구 뒤 오프라인 대장을 어떻게 대조할지 담당자별로 말로 확인한다.

이 리허설은 특정 국가의 공식 요구 사항을 그대로 적용하는 것이 아니라, 비상계획과 역할·의사소통 점검의 일반 원칙을 소규모 행사에 맞춰 적용한 것이다. NIST는 운영 우선순위와 비상계획 요구를 평가하도록 안내하고, CISA는 시나리오 기반 연습이 정보 공유·사고 대응·복구 절차를 개선하는 데 사용될 수 있다고 설명한다.[2] [3]

## References

[1]: https://pages.nist.gov/800-63-4/sp800-63a/privacy/ "NIST SP 800-63A-4 Privacy Considerations"
[2]: https://csrc.nist.gov/pubs/sp/800/34/r1/upd1/final "NIST SP 800-34 Rev. 1 Contingency Planning Guide"
[3]: https://www.cisa.gov/resources-tools/services/cisa-tabletop-exercise-packages "CISA Tabletop Exercise Packages"
[4]: https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html "OWASP Session Management Cheat Sheet"
[5]: https://cheatsheetseries.owasp.org/cheatsheets/Logging_Cheat_Sheet.html "OWASP Logging Cheat Sheet"
